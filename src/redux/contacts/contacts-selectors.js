export const itemsSelector = ({ contacts }) => contacts.items;
export const filterSelector = ({ contacts }) => contacts.filter;
export const loadingSelector = ({ contacts }) => contacts.loading;
export const errorSelector = ({ contacts }) => contacts.error;
